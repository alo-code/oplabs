# Copilot instructions for Beacon

The agent operating guide is **`AGENTS.md`** at the repo root — read it first (the five-stage
cadence, the repo map, the conventions), then `CADENCE.md`. This file is a pointer so Copilot
loads the same rules every other tool does.
